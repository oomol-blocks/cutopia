version: 0.0.9
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: poetry add pandas
  bootstrap: |
    npm install
    poetry install --no-root
dependencies:
  oomol-preview: 0.0.5
name: cutopia
description: Cutopia
displayName: Cutopia
repository: https://github.com/oomol-blocks/cutopia
releaseNotes: update icon
icon: ./icon.png
