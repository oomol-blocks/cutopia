version: 0.0.3
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: poetry add pandas
  bootstrap: |
    npm install
    poetry install --no-root
contributes:
  input_widgets:
    render: ./tasks/converter/bundled/inputRender.js
    widgets:
      - name: targetFormat
        json_schema:
          ui:options:
            title: TargetFormat
dependencies:
  oomol-preview: 0.0.5
name: cutopia
description: Cutopia
displayName: Cutopia
repository: https://github.com/oomol-blocks/cutopia
releaseNotes: fix build path
icon: ./icon.png
